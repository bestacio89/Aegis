<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================= COLOR TOKENS ================= -->
    <!-- Canvas / surfaces -->
    <SolidColorBrush x:Key="Brush.Ink"            Color="#0B0F17"/>
    <SolidColorBrush x:Key="Brush.Panel"          Color="#121826"/>
    <SolidColorBrush x:Key="Brush.PanelElevated"  Color="#161D2E"/>
    <SolidColorBrush x:Key="Brush.Line"           Color="#22293A"/>

    <!-- Text -->
    <SolidColorBrush x:Key="Brush.TextPrimary"    Color="#E8EDF4"/>
    <SolidColorBrush x:Key="Brush.TextMuted"      Color="#8993A6"/>

    <!-- Brand / structural accent — chrome only, never used for severity -->
    <SolidColorBrush x:Key="Brush.AccentStructural" Color="#4FA8D8"/>

    <!-- Semantic status — kept strictly separate from the brand accent above -->
    <SolidColorBrush x:Key="Brush.StatusCritical"   Color="#E5484D"/>
    <SolidColorBrush x:Key="Brush.StatusBlocker"    Color="#FF3B5C"/>
    <SolidColorBrush x:Key="Brush.StatusWarning"    Color="#E8A23D"/>
    <SolidColorBrush x:Key="Brush.StatusCompliant"  Color="#3DDC8B"/>

    <!-- ================= TYPOGRAPHY ================= -->
    <FontFamily x:Key="Font.Data">Consolas</FontFamily>
    <FontFamily x:Key="Font.Body">Segoe UI</FontFamily>

    <Style x:Key="Text.Eyebrow" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Body}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Padding" Value="0,0,0,0"/>
    </Style>

    <Style x:Key="Text.SectionTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Body}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
    </Style>

    <Style x:Key="Text.KpiValue" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Data}"/>
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
    </Style>

    <Style x:Key="Text.Title" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Body}"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
    </Style>

    <!-- ================= CARD STYLES ================= -->
    <Style x:Key="Card.Structural" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Line}"/>
        <Setter Property="BorderThickness" Value="1,0,1,1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="6"/>
    </Style>

    <Style x:Key="Card.TopRule" TargetType="Border">
        <Setter Property="Height" Value="3"/>
        <Setter Property="Background" Value="{StaticResource Brush.AccentStructural}"/>
        <Setter Property="CornerRadius" Value="4,4,0,0"/>
    </Style>

    <Style x:Key="Card.Kpi" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Brush.PanelElevated}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Line}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Width" Value="190"/>
        <Setter Property="Height" Value="92"/>
        <Setter Property="Padding" Value="14"/>
        <Setter Property="Margin" Value="6"/>
    </Style>

    <Style x:Key="StatusChip" TargetType="Border">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Height" Value="8"/>
        <Setter Property="CornerRadius" Value="2"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ================= DATAGRID ================= -->
    <Style x:Key="Grid.Ledger" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Brush.Ink}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowBackground" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.PanelElevated}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.Line}"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="RowHeight" Value="34"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Data}"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style x:Key="Grid.LedgerHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Brush.PanelElevated}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Body}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Line}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>

    <!-- ================= BUTTON ================= -->
    <Style x:Key="Button.Primary" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Brush.AccentStructural}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Ink}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Body}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="14,6"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="0.85"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Button.Icon"
       TargetType="Button">

        <Setter Property="FontFamily"
            Value="Segoe MDL2 Assets"/>

        <Setter Property="FontSize"
            Value="22"/>

        <Setter Property="Foreground"
            Value="{StaticResource Brush.TextPrimary}"/>

        <Setter Property="Background"
            Value="Transparent"/>

        <Setter Property="BorderThickness"
            Value="0"/>

        <Setter Property="HorizontalContentAlignment"
            Value="Center"/>

        <Setter Property="VerticalContentAlignment"
            Value="Center"/>

        <Setter Property="Cursor"
            Value="Hand"/>

    </Style>

</ResourceDictionary>